1. Должен быть максимально гибкий композитор, могут в процессе путем модификаций от пользователя добавляться новые узлы в сцене, изменяться взаимодействие со сценой.
2. Должны быть настолько же гибкие с системой плагинов seatop модули для взаимодействия со сценой.
3. Нужна api для добавления и изменения модулей и автоматической  их подгрузки в проект системой плагинов в config папке проекта.
4. Максимально консервативный input (Реализую максимально стабильно и как можно больше базовых функций ввода и всем что с ним связано, изменяется только seatop).
5. Несколько файлов конфигурации не учитывая плагины. Отдельный конфиг для инпута, действий, жестов и тд, отдельный общий конфиг рабочего окружения, отдельная конфигурация для окон, плагины для сцены, плагины для поведения элементов сцены при вводе (seatop) seat operation

